<section class="article-page section-padding">
  <div class="page-width">
    <div class="article-page__header">
      <h1 class="article-page__title">{{ page.title }}</h1>
    </div>
    <div class="article-page__content rte">
      {{ page.content }}
    </div>
  </div>
</section>
